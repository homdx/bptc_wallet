FROM chaoste/bptc
MAINTAINER Thomas Kellermeier <t-kellermeier@hotmail.de>

#-------------------------------------------------------------------------------
# Setup application
#-------------------------------------------------------------------------------

# Expose some ports to make them accessible from outside of the container
EXPOSE 8000
EXPOSE 8001

# Start CLI interface
CMD bash -c "python main.py -a"
